# Helm Chart Phase 3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Package all three services (task-hub MCP, crm-rest, crm-mcp) into a Helm chart that deploys to Kubernetes with ClusterIP Services, parameterizable via `--set` overrides.

**Architecture:** One Helm chart (`helm/lqc-mcp-mocks`) with a Deployment + ClusterIP Service per logical process. `crm-rest` and `crm-mcp` share the same container image but differ only in their `command`; this is expressed via a `command` field in `values.yaml`. All three are stateless (no PersistentVolume), so `replicas` can be freely scaled.

**Tech Stack:** Helm 3, Kubernetes 1.28+, `helm lint`, `helm template`, `kubectl apply --dry-run=client`

---

## File Map

| File | Role |
|------|------|
| `helm/lqc-mcp-mocks/Chart.yaml` | Chart metadata (name, version, appVersion) |
| `helm/lqc-mcp-mocks/values.yaml` | Default values for all three services |
| `helm/lqc-mcp-mocks/templates/_helpers.tpl` | `fullname` and `labels` named templates |
| `helm/lqc-mcp-mocks/templates/task-hub-deployment.yaml` | Deployment for task-hub MCP |
| `helm/lqc-mcp-mocks/templates/task-hub-service.yaml` | ClusterIP Service on port 3010 |
| `helm/lqc-mcp-mocks/templates/crm-rest-deployment.yaml` | Deployment for crm-rest (REST server) |
| `helm/lqc-mcp-mocks/templates/crm-rest-service.yaml` | ClusterIP Service on port 3011 |
| `helm/lqc-mcp-mocks/templates/crm-mcp-deployment.yaml` | Deployment for crm-mcp (MCP facade) |
| `helm/lqc-mcp-mocks/templates/crm-mcp-service.yaml` | ClusterIP Service on port 3012 |

---

## Task 1: Scaffold chart skeleton

**Files:**
- Create: `helm/lqc-mcp-mocks/Chart.yaml`
- Create: `helm/lqc-mcp-mocks/values.yaml`
- Create: `helm/lqc-mcp-mocks/templates/_helpers.tpl`

- [ ] **Step 1: Create Chart.yaml**

```yaml
# helm/lqc-mcp-mocks/Chart.yaml
apiVersion: v2
name: lqc-mcp-mocks
description: Mock MCP servers for task-hub and CRM
type: application
version: 0.1.0
appVersion: "1.0.0"
```

- [ ] **Step 2: Create values.yaml**

```yaml
# helm/lqc-mcp-mocks/values.yaml
image:
  taskHub: ghcr.io/liquidity/lqc-mock-task-hub:latest
  apiCrm:  ghcr.io/liquidity/lqc-mock-api-crm:latest
  pullPolicy: IfNotPresent

taskHub:
  replicas: 1
  port: 3010
  resources:
    requests: { cpu: "50m", memory: "64Mi" }
    limits:   { cpu: "200m", memory: "128Mi" }

crmRest:
  replicas: 1
  port: 3011
  command: ["node", "apps/api-crm/dist/main.js"]
  resources:
    requests: { cpu: "50m", memory: "64Mi" }
    limits:   { cpu: "200m", memory: "128Mi" }

crmMcp:
  replicas: 1
  port: 3012
  command: ["node", "apps/api-crm/dist/main-mcp.js"]
  resources:
    requests: { cpu: "50m", memory: "64Mi" }
    limits:   { cpu: "200m", memory: "128Mi" }
```

- [ ] **Step 3: Create _helpers.tpl**

```yaml
{{/* helm/lqc-mcp-mocks/templates/_helpers.tpl */}}
{{- define "lqc-mcp-mocks.labels" -}}
app.kubernetes.io/managed-by: {{ .Release.Service }}
helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
{{- end }}
```

- [ ] **Step 4: Verify helm lint passes**

```bash
helm lint helm/lqc-mcp-mocks
```

Expected output:
```
==> Linting helm/lqc-mcp-mocks
[INFO] Chart.yaml: icon is recommended

1 chart(s) linted, 0 chart(s) failed
```

- [ ] **Step 5: Commit**

```bash
git add helm/
git commit -m "feat(helm): scaffold chart skeleton with Chart.yaml, values.yaml, _helpers.tpl"
```

---

## Task 2: task-hub Deployment and Service

**Files:**
- Create: `helm/lqc-mcp-mocks/templates/task-hub-deployment.yaml`
- Create: `helm/lqc-mcp-mocks/templates/task-hub-service.yaml`

- [ ] **Step 1: Create task-hub-deployment.yaml**

```yaml
# helm/lqc-mcp-mocks/templates/task-hub-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-task-hub
  labels:
    app: task-hub
    {{- include "lqc-mcp-mocks.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.taskHub.replicas }}
  selector:
    matchLabels:
      app: task-hub
  template:
    metadata:
      labels:
        app: task-hub
    spec:
      containers:
        - name: task-hub
          image: {{ .Values.image.taskHub }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.taskHub.port }}
          resources: {{- toYaml .Values.taskHub.resources | nindent 12 }}
```

- [ ] **Step 2: Create task-hub-service.yaml**

```yaml
# helm/lqc-mcp-mocks/templates/task-hub-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-task-hub-svc
  labels:
    app: task-hub
    {{- include "lqc-mcp-mocks.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: task-hub
  ports:
    - port: {{ .Values.taskHub.port }}
      targetPort: {{ .Values.taskHub.port }}
      protocol: TCP
```

- [ ] **Step 3: Lint and render to verify output**

```bash
helm lint helm/lqc-mcp-mocks
helm template test-release helm/lqc-mcp-mocks | grep -A 20 "kind: Deployment"
```

Expected: A Deployment named `test-release-task-hub` with `containerPort: 3010` and a Service named `test-release-task-hub-svc`.

- [ ] **Step 4: Commit**

```bash
git add helm/lqc-mcp-mocks/templates/task-hub-deployment.yaml helm/lqc-mcp-mocks/templates/task-hub-service.yaml
git commit -m "feat(helm): add task-hub Deployment and ClusterIP Service"
```

---

## Task 3: crm-rest Deployment and Service

**Files:**
- Create: `helm/lqc-mcp-mocks/templates/crm-rest-deployment.yaml`
- Create: `helm/lqc-mcp-mocks/templates/crm-rest-service.yaml`

- [ ] **Step 1: Create crm-rest-deployment.yaml**

```yaml
# helm/lqc-mcp-mocks/templates/crm-rest-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-crm-rest
  labels:
    app: crm-rest
    {{- include "lqc-mcp-mocks.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.crmRest.replicas }}
  selector:
    matchLabels:
      app: crm-rest
  template:
    metadata:
      labels:
        app: crm-rest
    spec:
      containers:
        - name: crm-rest
          image: {{ .Values.image.apiCrm }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: {{- toYaml .Values.crmRest.command | nindent 12 }}
          ports:
            - containerPort: {{ .Values.crmRest.port }}
          resources: {{- toYaml .Values.crmRest.resources | nindent 12 }}
```

- [ ] **Step 2: Create crm-rest-service.yaml**

```yaml
# helm/lqc-mcp-mocks/templates/crm-rest-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-crm-rest-svc
  labels:
    app: crm-rest
    {{- include "lqc-mcp-mocks.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: crm-rest
  ports:
    - port: {{ .Values.crmRest.port }}
      targetPort: {{ .Values.crmRest.port }}
      protocol: TCP
```

- [ ] **Step 3: Lint and render to verify**

```bash
helm lint helm/lqc-mcp-mocks
helm template test-release helm/lqc-mcp-mocks | grep -A 5 "name: crm-rest"
```

Expected: Deployment `test-release-crm-rest` with `command: ["node", "apps/api-crm/dist/main.js"]` and `containerPort: 3011`.

- [ ] **Step 4: Commit**

```bash
git add helm/lqc-mcp-mocks/templates/crm-rest-deployment.yaml helm/lqc-mcp-mocks/templates/crm-rest-service.yaml
git commit -m "feat(helm): add crm-rest Deployment and ClusterIP Service"
```

---

## Task 4: crm-mcp Deployment and Service

**Files:**
- Create: `helm/lqc-mcp-mocks/templates/crm-mcp-deployment.yaml`
- Create: `helm/lqc-mcp-mocks/templates/crm-mcp-service.yaml`

- [ ] **Step 1: Create crm-mcp-deployment.yaml**

```yaml
# helm/lqc-mcp-mocks/templates/crm-mcp-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-crm-mcp
  labels:
    app: crm-mcp
    {{- include "lqc-mcp-mocks.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.crmMcp.replicas }}
  selector:
    matchLabels:
      app: crm-mcp
  template:
    metadata:
      labels:
        app: crm-mcp
    spec:
      containers:
        - name: crm-mcp
          image: {{ .Values.image.apiCrm }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: {{- toYaml .Values.crmMcp.command | nindent 12 }}
          ports:
            - containerPort: {{ .Values.crmMcp.port }}
          resources: {{- toYaml .Values.crmMcp.resources | nindent 12 }}
```

- [ ] **Step 2: Create crm-mcp-service.yaml**

```yaml
# helm/lqc-mcp-mocks/templates/crm-mcp-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-crm-mcp-svc
  labels:
    app: crm-mcp
    {{- include "lqc-mcp-mocks.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: crm-mcp
  ports:
    - port: {{ .Values.crmMcp.port }}
      targetPort: {{ .Values.crmMcp.port }}
      protocol: TCP
```

- [ ] **Step 3: Lint and render full chart**

```bash
helm lint helm/lqc-mcp-mocks
helm template test-release helm/lqc-mcp-mocks
```

Expected: 6 resources total — 3 Deployments, 3 Services. `crm-rest` and `crm-mcp` both use `image.apiCrm` but with different `command` values.

- [ ] **Step 4: Commit**

```bash
git add helm/lqc-mcp-mocks/templates/crm-mcp-deployment.yaml helm/lqc-mcp-mocks/templates/crm-mcp-service.yaml
git commit -m "feat(helm): add crm-mcp Deployment and ClusterIP Service"
```

---

## Task 5: Validate `--set` overrides and document port-forward usage

**Files:**
- Modify: `README.md` — add Phase 3 section

- [ ] **Step 1: Verify port override works**

```bash
helm template test-release helm/lqc-mcp-mocks \
  --set taskHub.port=9010 \
  | grep "containerPort"
```

Expected: one line with `containerPort: 9010` (task-hub overridden), others still 3011 / 3012.

- [ ] **Step 2: Verify image tag override works**

```bash
helm template test-release helm/lqc-mcp-mocks \
  --set image.taskHub=ghcr.io/liquidity/lqc-mock-task-hub:v2.0.0 \
  | grep "image:"
```

Expected: task-hub image is `ghcr.io/liquidity/lqc-mock-task-hub:v2.0.0`; api-crm image is still `ghcr.io/liquidity/lqc-mock-api-crm:latest`.

- [ ] **Step 3: Verify replicas override works**

```bash
helm template test-release helm/lqc-mcp-mocks \
  --set crmMcp.replicas=3 \
  | grep -A 2 "name: test-release-crm-mcp" | grep "replicas"
```

Expected: `replicas: 3` in the crm-mcp Deployment.

- [ ] **Step 4: Add Phase 3 section to README.md**

Append the following to the **bottom** of `README.md`:

```markdown
## Phase 3 — Kubernetes via Helm

### Install

```bash
helm install lqc-mcp-mocks ./helm/lqc-mcp-mocks
```

### Connect Claude Code (via port-forward)

```bash
kubectl port-forward svc/lqc-mcp-mocks-task-hub-svc 3010:3010 &
kubectl port-forward svc/lqc-mcp-mocks-crm-mcp-svc 3012:3012 &
# claude mcp add commands are unchanged — still localhost:PORT
```

### In-cluster access

Other pods reach the MCP servers at:
- `http://lqc-mcp-mocks-task-hub-svc:3010/mcp`
- `http://lqc-mcp-mocks-crm-mcp-svc:3012/mcp`

### Common overrides

```bash
# scale crm-mcp to 3 replicas (stateless, safe to scale)
helm upgrade lqc-mcp-mocks ./helm/lqc-mcp-mocks --set crmMcp.replicas=3

# pin image tags for a release
helm upgrade lqc-mcp-mocks ./helm/lqc-mcp-mocks \
  --set image.taskHub=ghcr.io/liquidity/lqc-mock-task-hub:v1.2.0 \
  --set image.apiCrm=ghcr.io/liquidity/lqc-mock-api-crm:v1.2.0
```
```

- [ ] **Step 5: Final helm lint**

```bash
helm lint helm/lqc-mcp-mocks
```

Expected: `1 chart(s) linted, 0 chart(s) failed`

- [ ] **Step 6: Commit**

```bash
git add helm/ README.md
git commit -m "docs: add Phase 3 Helm usage to README and validate --set overrides"
```

---

## Spec Coverage Check

| PRD requirement | Task |
|---|---|
| `helm/lqc-mcp-mocks/Chart.yaml` | Task 1 |
| `helm/lqc-mcp-mocks/values.yaml` | Task 1 |
| task-hub Deployment + Service | Task 2 |
| crm-rest Deployment + Service | Task 3 |
| crm-mcp Deployment + Service | Task 4 |
| `crm-rest` and `crm-mcp` share image, different `command` | Tasks 3 & 4 via `values.yaml` |
| `helm install lqc-mcp-mocks ./helm/lqc-mcp-mocks` deploys all resources | Task 5 Step 5 |
| Port-forwarded MCP servers respond to Claude Code | Task 5 Step 4 (docs) |
| `--set` overrides for ports and image tags work | Task 5 Steps 1-3 |
| No PersistentVolume (in-memory data) | N/A — omitted by design |
| `replicas > 1` safe (stateless sessions) | Task 1 values.yaml + Task 5 Step 3 |
